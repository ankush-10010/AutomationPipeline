# Core pipeline dependencies (local CPU)
# Install with: pip install -r requirements.txt

# LLM client (talks to Ollama)
requests>=2.31.0

# Audio captioning (CPU-optimized Whisper)
faster-whisper>=1.0.0

# Video processing
moviepy>=1.0.3

# Image processing (thumbnails, overlays)
Pillow>=10.0.0

# YouTube API
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# Config
pyyaml>=6.0.1

# TTS (local dev only — Piper)
piper-tts>=1.2.0

# Utilities
tqdm>=4.66.0
python-dateutil>=2.8.2
